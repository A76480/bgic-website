# Copy this file to .env and fill in real values before running

# Password admin uses to log into the admin panel
ADMIN_PASSWORD=bgic2026

# Port the server runs on (most hosts set this automatically)
PORT=3000

# Paystack keys (add these when you're ready to accept real payments)
PAYSTACK_PUBLIC_KEY=
PAYSTACK_SECRET_KEY=
